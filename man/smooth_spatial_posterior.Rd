% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_dependence.R
\name{smooth_spatial_posterior}
\alias{smooth_spatial_posterior}
\title{Smooth spatial posterior curves using neighbouring cells}
\usage{
smooth_spatial_posterior(
  spatial_posterior,
  rho = 0.25,
  adjacency = NULL,
  iterations = 1,
  eps = 1e-06
)
}
\arguments{
\item{spatial_posterior}{Matrix/list returned by
\code{\link[=spatial_posterior_probability_extinction_varying_end_year]{spatial_posterior_probability_extinction_varying_end_year()}}.}

\item{rho}{Numeric value between 0 and 1 controlling neighbour influence.
\code{rho = 0} returns the independent local posteriors. Larger values borrow
more information from neighbouring cells.}

\item{adjacency}{Optional adjacency list. If \code{NULL}, rook neighbours are used
for the dimensions of \code{spatial_posterior}.}

\item{iterations}{Number of smoothing iterations.}

\item{eps}{Small value used to keep probabilities away from 0 and 1 before
applying the logit transform.}
}
\value{
Matrix/list of smoothed posterior probability vectors.
}
\description{
This function applies a transparent neighbour-informed adjustment to spatial
posterior probability curves. It is a spatial smoothing method, not a full
hierarchical spatial model.
}
\examples{
grid <- list(c(1880, 1883, 1895), c(1881, 1884, 1892), NA, c(1882, 1884, 1896))
dim(grid) <- c(2, 2)
posterior <- spatial_posterior_probability_extinction_varying_end_year(grid, 1880, 1900)
smooth_spatial_posterior(posterior, rho = 0.25)
}
